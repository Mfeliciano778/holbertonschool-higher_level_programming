#!/usr/bin/python3
'''is_same_class returns boolean if object is an instance of a class'''


def is_same_class(obj, a_class):
    '''is_same_class returns boolean if object is an instance of a class'''
    return type(obj) == a_class
